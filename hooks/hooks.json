{
  "description": "Frostbyte grounding — on session start, if the repo is linked via .frostbyte.json, tell the agent which Frostbyte project it is working in. Intentionally unmatcher-ed so it fires on every SessionStart event (startup, resume, clear, compact): grounding must survive resumes and context compaction.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${CLAUDE_PLUGIN_ROOT}/hooks/frostbyte-grounding.sh\""
          }
        ]
      }
    ]
  }
}
